name: 🚀 Deploy to iNET Hosting

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest 

    steps:

      - name: 📦 Checkout source code
        uses: actions/checkout@v4

      - name: 🚀 Upload to iNET via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          server-dir: /public_html/         # Thư mục gốc web trên hosting iNET
          local-dir: ./                     # Upload toàn bộ project hiện tại
          exclude: |                        # Loại trừ các file không cần up
            **/.git*
            **/.github*
            **/node_modules/**
            **/*.md
            **/README*
            **/.vscode*
